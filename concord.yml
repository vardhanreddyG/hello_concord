flows:
  default:
    - log: "hello concord"
    - form: myForm
    - log: "hello ${myForm.name}"
    - log: "form submittedby ${myForm.submittedBy.displayName}"
forms:
  myForm:
    - name: { label: "name", type: "string" }
